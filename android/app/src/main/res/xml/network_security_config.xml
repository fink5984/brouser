<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Production/staging: HTTPS only, system + user trust anchors, no
         cleartext anywhere. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Local development only: lets debug builds reach a backend running
         on the host machine's plain-HTTP dev server via the emulator's
         10.0.2.2 alias. Never matches a real production domain. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
